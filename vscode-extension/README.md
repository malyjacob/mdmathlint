# mdmathlint VS Code Extension Prototype

This extension starts the bundled `mdmathlint-lsp` server for Markdown documents. During development, open this directory as a VS Code extension project or point `mdmathlint.serverPath` at an already-built `dist/lsp.js`.
